/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images:{
    domains: ['lh3.googleusercontent.com', 'cdn.waifu.im', 'res.cloudinary.com', 'i.waifu.pics', 'picsum.photos']
  }
};

export default nextConfig;
